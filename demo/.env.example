# Get a key at https://console.groq.com/keys
GROQ_API_KEY=

# Optional. Leave unset and the server picks the best model your account
# actually has — it checks Groq's live model list at startup.
# GROQ_MODEL=openai/gpt-oss-120b
PORT=4173
